<nav class="navbar navbar-expand-md navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to "Systems", root_path, class: "navbar-brand" %>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <% unless user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link #{active?(new_user_session_path)}" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Home", root_path, class: "nav-link #{active?(root_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to "Systems", systems_path, class: "nav-link #{active?(systems_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to "Customers", customers_path, class: "nav-link #{active?(customers_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, data: {confirm: "Are you sure?"}, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>